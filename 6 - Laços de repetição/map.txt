keys: map, foreach, repetição, laço de repetião, laço, for

var oneBetterThanNums = nums.map(function (num) {
    return num + 1;
});



//.forEach() has no limitations as opposed to .map(), since they're both designed to work differently.

.forEach() -> for each value V of array A do X (e.g.: console.log(v))
.map() -> map (transform) each value V of array A to X (e.g.: v + C)
